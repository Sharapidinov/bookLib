export enum BOOK_CATEGORIES {
  ALL = "all",
  ART = "art",
  BIOGRAPHY = "biography",
  COMPUTERS = "computers",
  HISTORY = "history",
  MEDICAL = "medical",
  POETRY = "poetry",
}
export enum SORT_TYPES {
  RELEVANCE = "relevance",
  NEWEST = "newest",
}

export const PAGINATION_STEP = 30;
